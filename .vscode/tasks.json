{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and run backend",
            "type": "shell",
            "command": "cd backend && go build && ./backend server",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}